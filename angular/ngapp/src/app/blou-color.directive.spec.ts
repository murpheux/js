import { BlouColorDirective } from './blou-color.directive';

describe('BlouColorDirective', () => {
  it('should create an instance', () => {
    const directive = new BlouColorDirective();
    expect(directive).toBeTruthy();
  });
});
